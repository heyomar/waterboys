var $ = require('jquery');

var slick = require('slick-carousel')

module.exports = () => {
  $(document).ready(function(){
    $('#player-slider').slick({
      setting-name: setting-value
    });
  });
}
